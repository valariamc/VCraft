From 3a4c39a36f2e325acc0fc71414e9012b666f3cdd Mon Sep 17 00:00:00 2001
From: Aikar <aikar@aikar.co>
Date: Tue, 27 Mar 2018 04:31:35 +0100
Subject: [PATCH] Add a call helper to Event

Reduces diff in Server patches

diff --git a/src/main/java/org/bukkit/event/Event.java b/src/main/java/org/bukkit/event/Event.java
index 6677e1bd..572dcabb 100644
--- a/src/main/java/org/bukkit/event/Event.java
+++ b/src/main/java/org/bukkit/event/Event.java
@@ -1,5 +1,6 @@
 package org.bukkit.event;
 
+import org.bukkit.Bukkit;
 import org.bukkit.plugin.PluginManager;
 
 /**
@@ -33,6 +34,22 @@ public abstract class Event {
         this.async = isAsync;
     }
 
+    // Alphheim start - Paper end
+    /**
+     * Calls the event and tests if cancelled.
+     *
+     * @return if event was cancelled, if cancellable. otherwise true.
+     */
+    public boolean callEvent() {
+        Bukkit.getPluginManager().callEvent(this);
+        if (this instanceof Cancellable) {
+            return !((Cancellable) this).isCancelled();
+        } else {
+            return true;
+        }
+    }
+    // Alphheim end - Paper end
+
     /**
      * Convenience method for providing a user-friendly identifier. By
      * default, it is the event's class's {@linkplain Class#getSimpleName()
@@ -75,6 +92,8 @@ public abstract class Event {
         return async;
     }
 
+
+
     public enum Result {
 
         /**
-- 
2.16.2

